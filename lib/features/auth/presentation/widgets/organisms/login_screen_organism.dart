import 'package:flutter/material.dart';

class LoginScreenOrganism extends StatelessWidget {
  const LoginScreenOrganism({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      child: Container(),
    );
  }
}

